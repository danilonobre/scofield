<!-- mentor:file
The component inventory. Every reusable UI component in the product should be registered here. The AI uses this map when composing screens — it checks here before inventing new components. An incomplete map leads to duplicate components with different names, inconsistent behavior, and visual drift.
priority: medium

extensions:
- components-sync: Syncs component specs from individual Figma node URLs. Run after updating designs in Figma to keep component specs current.
-->

# Component Map

<!-- mentor
A table of all UI components in the product. Each component should have a description specific enough to distinguish it from similar components. Status tracks whether it has been built or is still planned — this is used during implementation to know what exists vs what needs to be created.

quality signals:
- Every component referenced in screen specs is listed here
- Descriptions distinguish between similar components (e.g. "primary button" vs "ghost button")
- Status is kept current — built components are marked built
- Component names match the actual file names in the codebase
-->

All UI components in this product.

| Component | Description | Status |
|-----------|-------------|--------|
| | | |
