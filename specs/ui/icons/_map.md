<!-- mentor:file
The icon inventory. Documents the icon library in use and tracks which icons are used where. For products using a standard library (Phosphor, Lucide), this is a usage log. For products with custom icons, this is the registry that drives individual icon spec files. Keep this in sync with Figma using /icons-sync.
priority: low

extensions:
- icons-sync: Extracts the icon inventory from a Figma page or section and updates this map automatically.
-->

# Icon Map

<!-- mentor
Document the icon library and the icons in use. The Figma source field should be filled if icons are managed in a Figma file — this enables /icons-sync. Custom icons require individual spec files in this directory; standard library icons do not.

quality signals:
- Library is named explicitly (not "various")
- Every icon used in the product is listed with its usage context
- Custom icons are marked and have corresponding individual spec files
- Icons used in navigation or high-frequency contexts are identified as such
-->

**Library:**
**Figma source:**

| Icon name | Usage | Custom? |
|-----------|-------|---------|
| | | |
