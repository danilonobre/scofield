<!-- mentor:file
Template for individual custom icon spec files. Only create individual icon files for custom icons — standard library icons are tracked in _map.md only. Custom icon specs define usage context, size variants, and any constraints that differ from the standard library defaults.
priority: low
-->

# [IconName]

<!-- mentor:file
Replace [IconName] with the actual icon name. Only use this template for custom icons. Standard library icons (Phosphor, Lucide, etc.) do not need individual spec files.
priority: low
-->

**figma_url:**

## Usage

<!-- mentor
Where and how this icon is used in the product. Be specific — "used in the navigation sidebar to represent projects" is better than "navigation icon". If the icon has multiple usage contexts, list them.

quality signals:
- All usage contexts are listed
- Size at each usage context is noted
- Color behavior is described (inherits from parent? fixed color? changes on hover?)
-->

## Sizes

<!-- mentor
What sizes are exported or supported. For custom icons, this determines what SVG files need to be created. Sizes should match the spacing scale from _tokens.md where possible.

quality signals:
- All exported sizes are listed with pixel values
- The intended usage context for each size is noted
-->

## Notes

<!-- mentor
Any usage constraints, animation behavior, or guidelines specific to this icon.

quality signals:
- Notes are only present if there is something non-obvious to document
- Constraints are stated as requirements
-->
